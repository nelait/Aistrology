# Files never uploaded to Railway by `railway up`.
# This project has no .git, so the upload can't fall back to .gitignore —
# be explicit, above all about .env (local secrets) and node_modules (size).

.env
.env.*
!.env.example

node_modules/
dist/
dist-ssr/

scratch/
*.local
.DS_Store

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.vscode/
.idea/
.claude/
